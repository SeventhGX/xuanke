server:
  port: 8050
spring:
  application:
    name: client
  thymeleaf:
    prefix: class:/static
    suffix: .html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
